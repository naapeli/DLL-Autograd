[project]
name = "dll-autograd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pybind11>=3.0.2",
    "pybind11-stubgen>=2.5.5",
    "pytest>=9.0.2",
]
